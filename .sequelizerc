const path = require("path");

module.exports = {
  config: path.resolve("dist", "config", "config.js"),
  "models-path": path.resolve("dist", "main", "backend", "models"),
  "seeders-path": path.resolve("dist", "main", "backend", "seeders"),
  "migrations-path": path.resolve("dist", "main", "backend", "migrations"),
};
